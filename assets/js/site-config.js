/* ============================================================
 *  網站資料來源設定
 * ============================================================
 *
 *  ⚠️ 重要：閱讀前先了解資安模型
 *  ----------------------------------------------------------
 *  下方的 GOOGLE_SHEETS_ID 不是密碼，是「公開的入口」。
 *  網站要能讀，就一定要把 ID 寫在這支 JS 裡；這支 JS 又會被
 *  每個瀏覽器訪客抓走，所以 ID 等於「跟 LINE OA URL 同等級
 *  的公開連結」，**寫死跟不寫死沒差別**。
 *
 *  ⚠️ 因此真正的資安重點不是「藏 ID」，而是「拿到 ID 的人
 *  能看到什麼」。請務必遵守下面三條：
 *
 *  1. 同一份試算表的所有 sheet 都會曝光
 *     即使你在試算表新加一個叫「本月成本」的 sheet、
 *     即使 csv.js 的 SHEETS 列表沒列它 ——
 *     任何人都能用以下網址直接抓走：
 *       https://docs.google.com/spreadsheets/d/{ID}/gviz/tq
 *         ?tqx=out:csv&sheet=本月成本
 *
 *  2. 內部資料一律用「完全分開」的另一份試算表
 *     不是「同一檔的不同 sheet」，是「不同檔案」。
 *     內部那份分享設定請用「指定 email 邀請」，**絕不可**
 *     使用「擁有連結者可檢視」。
 *
 *  3. 學員個資、付款、講師費、API token、密碼
 *     全部不要進這份網站試算表（也不要進 repo）
 *
 *  詳見 README 的「資安守則」段。
 *
 * ============================================================
 *  使用方式
 * ----------------------------------------------------------
 *  留空時：網站讀本地 data/*.csv（開發、離線備援）
 *  填入 ID：網站改抓那份試算表的線上資料
 *
 *  設定步驟：
 *    1. 把 data/site.xlsx 上傳到 Google Drive
 *       → 右鍵「Open with → Google Sheets」
 *    2. 試算表分享 = 「擁有連結者 → 檢視者」
 *       （不要設「可編輯」，否則任何人都能改）
 *    3. 從網址列複製 ID：
 *       https://docs.google.com/spreadsheets/d/[← 這段 →]/edit
 *    4. 貼到下面 GOOGLE_SHEETS_ID 的引號內、commit & push
 *    5. 約 5 分鐘內網站自動套用（Google CDN 快取）
 *
 *  想暫時切回本地 CSV：把 GOOGLE_SHEETS_ID 改回空字串再 push
 *
 *  想換不同試算表：直接改 ID 推上去。舊試算表如果含敏感資料，
 *  記得把分享關掉 / 刪檔，否則 ID 已外流的事實是無法收回的。
 * ============================================================ */
window.SITE_CONFIG = {
  GOOGLE_SHEETS_ID: '1G6xPt-fnZl_gSn2hwsD0vlY44EGVU49X',
};
